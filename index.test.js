const handler = require()

describe ('prueba', ()=> {
    describe ('suma', ()=> {
        it("suma 2 nuemros", ()=> {
            const suma = (a, b) => {
                return a + b
            }

            expect(suma(1,2)).toEqual(3)

        })

    })  

})